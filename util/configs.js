const defaults = {
  formatTables: false,
  imageLinksExistence: true,
  timezone: 'UTC',
  dateFormat: 'ddd, D MMMM YYYY, h:mm A z',
  dateFallback: false,
  timeFallback: false
}

module.exports = defaults
